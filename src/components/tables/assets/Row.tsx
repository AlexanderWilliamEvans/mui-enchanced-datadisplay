import React from "react";

type rowProps = {
    data: Array<string>;
    id: string | number;
    
};

const Row:React.FC<rowProps> = (props) => {

    return (
        <React.Fragment>
            
        </React.Fragment>
    );
};
export default Row;